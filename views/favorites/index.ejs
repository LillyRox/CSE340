<% if (title) { %>
  <h1><%= title %></h1>
<% } %>

<% if (messages && messages.length) { %>
  <% messages.forEach(function(m) { %>
    <p class="flash-message"><%= m %></p>
  <% }) %>
<% } %>

<%- favorites %>
